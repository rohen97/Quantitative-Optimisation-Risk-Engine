project_name: The Wolf Quant Model
random_seed: 42
base_currency: USD
output_dir: reports/outputs
current_portfolio_path: data/external/current_portfolio_template.csv
mock_data:
  enabled: true
  securities: 24
  price_days: 756
data_provider:
  name: mock
  alpaca:
    enabled: false
    env: paper
    data_feed: iex
    lookback_days: 756

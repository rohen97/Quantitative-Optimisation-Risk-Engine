# The Wolf Quant Model

Portfolio-aware, regime-aware and sentiment-aware conservative listed-equity selection MVP for DACH, Mainland China, Hong Kong and India.

Run the mock-data pipeline:

```bash
python scripts/run_full_pipeline.py
```

Run tests:

```bash
pytest
```

Outputs are saved in `reports/outputs/`.
